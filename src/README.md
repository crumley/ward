# Ward Source

The **code** leg. Implements the realization described in [`../design/`](../design/), under the
constraints set by [`../intent/`](../intent/).

> **Status:** placeholder until the first implementation pass. No code exists yet.

Lay out source here to **mirror the design's organization** — one module per seam/mechanism plus a
store core, following [`../design/00-foundation.md`](../design/00-foundation.md). The concrete
module layout is itself a **design** decision; record it in `../design/00-foundation.md` before
populating this tree.

`src` moves with `design` and `test`; all three reconcile back to `intent` when they diverge.
