// Ward's own version stamp (§14: a workspace records which Ward created/updated
// it). Kept in step with package.json; SCHEMA_VERSION bumps only on a structural
// change migration must reason about.
export const WARD_VERSION = '0.2.0';
export const SCHEMA_VERSION = 1;
