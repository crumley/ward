{
  "name": "ward",
  "version": "0.2.0",
  "description": "Ward — a CLI for operating opinionated, structured human+agent workspaces.",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "bin": {
    "ward": "src/cli/index.ts"
  },
  "scripts": {
    "ward": "node src/cli/index.ts",
    "test": "node --test \"test/**/*.test.ts\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^14.0.1",
    "yaml": "^2.8.1",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.2",
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
