# Design: Model Selection

> **Layer:** design — one realization. Names tools/structures; may change. **Status:** placeholder —
> to be filled during implementation.

## Governed by

`../../intent/02-subsystems/04-model-selection.md` — the per-scope override hierarchy and the
fast-vs-deep intent; model-agnostic.

## Realization (to fill)

- The **configuration shape** for defaults and per-scope overrides.
- The initial **persona→tier mapping** and the concrete **model ids** behind "fast" and "deep"
  (tracking the current frontier).
- How **thinking depth** is expressed.

## Blanks to settle

- See `../blanks-register.md` (config shape; persona→tier mapping; model ids; depth expression).
