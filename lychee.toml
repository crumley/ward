# lychee — broken-link checker for Ward docs.
#
# Every link in this repo is an internal relative path today, so we run in
# offline mode: lychee verifies that each linked file exists on disk and makes
# no network requests (fast, deterministic, CI-safe). If external URLs are
# added later, drop `offline` for a scheduled run — see the Makefile.

# Only check the local filesystem; never hit the network.
offline = true

# Validate `#heading` anchor fragments against the target document's headings.
include_fragments = "anchor-only"

# Don't descend into VCS internals.
exclude_path = ['\.git/']
